# ASXR-PS1

## MX2LM Unified Artifact

This repository provides the single-file MX2LM PowerShell artifact.

- `mx2lm.ps1` contains the frozen, sectioned layout described in the unified plan.
- Use `-ci` to emit the conformance snapshot.
